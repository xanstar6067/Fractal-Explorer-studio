<Window x:Class="FractalExplorerWPF.Views.Ifs3DWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:theme="clr-namespace:FractalExplorerWPF.Theming"
        Title="Конструктор объёмных IFS" Width="1190" Height="760" MinWidth="920" MinHeight="600"
        WindowStartupLocation="CenterOwner" Closing="Window_OnClosing">
    <Grid Background="{DynamicResource Theme.BaseBackgroundBrush}">
        <Grid.ColumnDefinitions><ColumnDefinition Width="350"/><ColumnDefinition/></Grid.ColumnDefinitions>
        <Border Margin="10" Padding="14" Background="{DynamicResource Theme.PanelBackgroundBrush}"
                BorderBrush="{DynamicResource Theme.BorderBrush}" BorderThickness="1" CornerRadius="7">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock FontSize="18" FontWeight="SemiBold" Text="Конструктор объёмных IFS"/>
                    <TextBlock Margin="0,8,0,3" Text="Готовый набор"/>
                    <ComboBox x:Name="PresetBox" SelectionChanged="Preset_OnChanged"/>
                    <TextBlock Margin="0,12,0,3" Text="Итерации"/>
                    <TextBox x:Name="IterationsBox" TextChanged="Parameter_OnChanged"/>
                    <TextBlock Margin="0,12,0,3" Text="Преобразования пространства" FontWeight="SemiBold"/>
                    <ListBox x:Name="TransformList" Height="105" SelectionChanged="TransformList_OnChanged">
                        <ListBox.ItemTemplate>
                            <DataTemplate><TextBlock Padding="4" Text="{Binding Probability, StringFormat='Вес: {0:G4}'}"/></DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <Button Content="+ Добавить" Click="AddTransform_OnClick"/>
                        <Button Content="Дублировать" Click="DuplicateTransform_OnClick"/>
                        <Button Content="Удалить" Click="DeleteTransform_OnClick"/>
                    </StackPanel>
                    <TextBlock Margin="0,12,0,3" Text="Матрица 3×3, перенос и вероятность" FontWeight="SemiBold"/>
                    <TextBlock Foreground="{DynamicResource Theme.SecondaryTextBrush}" Text="Строки сверху вниз: x′, y′, z′ = M · (x, y, z) + t"/>
                    <Grid Margin="0,7,0,0">
                        <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
                        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="x" HorizontalAlignment="Center"/><TextBlock Grid.Row="0" Grid.Column="1" Text="y" HorizontalAlignment="Center"/><TextBlock Grid.Row="0" Grid.Column="2" Text="z" HorizontalAlignment="Center"/><TextBlock Grid.Row="0" Grid.Column="3" Text="перенос" HorizontalAlignment="Center"/>
                        <TextBox x:Name="M11Box" Grid.Row="1" Margin="2" TextChanged="Matrix_OnChanged"/><TextBox x:Name="M12Box" Grid.Row="1" Grid.Column="1" Margin="2" TextChanged="Matrix_OnChanged"/><TextBox x:Name="M13Box" Grid.Row="1" Grid.Column="2" Margin="2" TextChanged="Matrix_OnChanged"/><TextBox x:Name="TxBox" Grid.Row="1" Grid.Column="3" Margin="2" TextChanged="Matrix_OnChanged"/>
                        <TextBox x:Name="M21Box" Grid.Row="2" Margin="2" TextChanged="Matrix_OnChanged"/><TextBox x:Name="M22Box" Grid.Row="2" Grid.Column="1" Margin="2" TextChanged="Matrix_OnChanged"/><TextBox x:Name="M23Box" Grid.Row="2" Grid.Column="2" Margin="2" TextChanged="Matrix_OnChanged"/><TextBox x:Name="TyBox" Grid.Row="2" Grid.Column="3" Margin="2" TextChanged="Matrix_OnChanged"/>
                        <TextBox x:Name="M31Box" Grid.Row="3" Margin="2" TextChanged="Matrix_OnChanged"/><TextBox x:Name="M32Box" Grid.Row="3" Grid.Column="1" Margin="2" TextChanged="Matrix_OnChanged"/><TextBox x:Name="M33Box" Grid.Row="3" Grid.Column="2" Margin="2" TextChanged="Matrix_OnChanged"/><TextBox x:Name="TzBox" Grid.Row="3" Grid.Column="3" Margin="2" TextChanged="Matrix_OnChanged"/>
                        <TextBlock Grid.Row="4" Grid.ColumnSpan="2" Margin="2,6,0,0" Text="Вес выбора"/>
                        <TextBox x:Name="ProbabilityBox" Grid.Row="5" Grid.ColumnSpan="4" Margin="2" TextChanged="Matrix_OnChanged"/>
                    </Grid>
                    <Separator Margin="0,12"/>
                    <TextBlock Text="Вид" FontWeight="SemiBold"/>
                    <UniformGrid Columns="2" Margin="0,4,0,0">
                        <TextBlock Text="Азимут, °"/><TextBox x:Name="YawBox" TextChanged="Parameter_OnChanged"/>
                        <TextBlock Text="Наклон, °"/><TextBox x:Name="PitchBox" TextChanged="Parameter_OnChanged"/>
                        <TextBlock Text="Масштаб"/><TextBox x:Name="ZoomBox" TextChanged="Parameter_OnChanged"/>
                    </UniformGrid>
                    <Button Margin="0,6,0,0" Content="Сбросить вид" Click="ResetView_OnClick"/>
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <Button Content="Цвет точек" Click="PointColor_OnClick"/>
                        <Border x:Name="PointColorSwatch" Width="28" Margin="5,2,10,2" CornerRadius="3"/>
                        <Button Content="Цвет фона" Click="BackgroundColor_OnClick"/>
                        <Border x:Name="BackgroundColorSwatch" Width="28" Margin="5,2" CornerRadius="3"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                        <Button Content="Сохранения" Click="Saves_OnClick"/>
                        <Button Content="Экспорт изображения" Click="Export_OnClick"/>
                    </StackPanel>
                    <ProgressBar x:Name="RenderProgress" Height="8" Margin="0,12,0,0" Maximum="100"/>
                    <TextBlock x:Name="StatusText" Margin="0,7,0,0" TextWrapping="Wrap" Foreground="{DynamicResource Theme.SecondaryTextBrush}"/>
                    <TextBlock Margin="0,10,0,0" TextWrapping="Wrap" Foreground="{DynamicResource Theme.DisabledTextBrush}"
                               Text="ЛКМ: повернуть. ПКМ: сдвинуть. Колесо: масштаб. Изменения преобразований запускают новый рендер."/>
                </StackPanel>
            </ScrollViewer>
        </Border>
        <Border x:Name="CanvasHost" Grid.Column="1" Margin="0,10,10,10" Background="Black" ClipToBounds="True"
                BorderBrush="{DynamicResource Theme.BorderBrush}" BorderThickness="1" CornerRadius="7"
                theme:ThemeContract.IgnoreAudit="True" SizeChanged="Canvas_OnSizeChanged"
                MouseLeftButtonDown="Canvas_OnMouseDown" MouseRightButtonDown="Canvas_OnMouseDown"
                MouseMove="Canvas_OnMouseMove" MouseLeftButtonUp="Canvas_OnMouseUp"
                MouseRightButtonUp="Canvas_OnMouseUp" MouseWheel="Canvas_OnMouseWheel">
            <Grid x:Name="SavePreviewLayer"><Image x:Name="CurrentImage" Stretch="Fill"/></Grid>
        </Border>
    </Grid>
</Window>
